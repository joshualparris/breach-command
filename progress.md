Original prompt: Improve every deployable app on this computer, verify it, and publish it.

## 2026-06-09

- Added a fullscreen title-screen control and `F` keyboard shortcut.
- Added concise `window.render_game_to_text()` and deterministic `window.advanceTime(ms)` hooks.
- Fixed stale relative imports in the included Node test suite.
- Node content, integration, and passive battle tests passed.
- Browser state inspection and route-map screenshot passed without console errors.
